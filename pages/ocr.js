import Layout from '../components/layout';
import Header from '../components/header';

export default () => (
  <div>
    <Layout>
      <Header />
      <main>
        <h1>Start</h1>
        <p>let's solve the captcha!</p>

        <h2>Step1: Preprocess</h2>
        <p>The first step is binarization</p>
      </main>
    </Layout>
  </div>
);
